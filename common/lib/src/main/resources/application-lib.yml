file:
  rootDir: /opt/
  ## BIRT로 생성되어 업로드 할 PDF 저장 기준 위치
  baseDir: ${file.rootDir}/upload


aws:
  credential.profile:
  kms:
    arn: arn:aws:kms:ap-northeast-2:000000000000:key/test
  ses:
    region:
    sendMail:
  s3:
    signingRegion:
    bucketName:
    URL:

--- # develop
spring:
  config:
    activate:
      on-profile: develop
aws:
  s3:
    bucketName:

--- # product
spring:
  config:
    activate:
      on-profile: product
file.rootDir:

aws:
  credential.profile:
  s3:
    bucketName:


--- # test
spring:
  config:
    activate:
      on-profile: test

file.rootDir:

aws:
  credential.profile:
  s3:
    bucketName:
